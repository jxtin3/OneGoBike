<form class="form-panel" action="{{ $action }}" method="POST" enctype="multipart/form-data">@csrf @if($method !== 'POST') @method($method) @endif <label>Title<input name="title" required value="{{ old('title',$picture?->title) }}"></label><label>Description<textarea name="description" rows="6">{{ old('description',$picture?->description) }}</textarea></label><label>{{ $picture ? 'Replace picture' : 'Picture' }}<input type="file" name="image" accept="image/jpeg,image/png,image/webp" {{ $picture ? '' : 'required' }}></label>@if($picture)<img class="form-preview" src="{{ asset('storage/'.$picture->image_path) }}" alt="">@endif<div class="form-actions"><button class="primary-button">{{ $button }}</button><a class="map-button" href="{{ route('admin.operations.pictures.index') }}">Cancel</a></div></form>
