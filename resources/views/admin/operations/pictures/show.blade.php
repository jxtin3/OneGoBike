@extends('admin.operations.layout') @section('title','Picture Details') @section('heading','Picture details') @section('content')<article class="detail-panel"><img class="detail-image" src="{{ asset('storage/'.$picture->image_path) }}" alt="{{ $picture->title }}"><h2>{{ $picture->title }}</h2><p>{{ $picture->description }}</p><p>Uploaded by: {{ $picture->uploader?->name ?? 'Unknown' }} · {{ $picture->created_at->format('F d, Y') }}</p><div class="form-actions"><a class="primary-button" href="{{ route('admin.operations.pictures.edit',$picture) }}">Edit</a><a class="map-button" href="{{ route('admin.operations.pictures.index') }}">Back</a></div></article>@endsection
