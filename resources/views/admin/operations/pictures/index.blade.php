@extends('admin.operations.layout') @section('title','Pictures') @section('heading','Picture management') @section('content')<div class="page-actions"><form class="search-form"><input name="search" value="{{ request('search') }}" placeholder="Search pictures..."><button class="map-button">Search</button></form><a class="primary-button" href="{{ route('admin.operations.pictures.create') }}">+ Add picture</a></div><div class="picture-grid">@forelse($pictures as $picture)<article class="picture-card"><img src="{{ asset('storage/'.$picture->image_path) }}" alt="{{ $picture->title }}"><div><h2>{{ $picture->title }}</h2><p>{{ Str::limit($picture->description,90) }}</p><small>{{ $picture->created_at->format('M d, Y') }}</small><div class="actions"><a href="{{ route('admin.operations.pictures.show',$picture) }}">View</a><a href="{{ route('admin.operations.pictures.edit',$picture) }}">Edit</a><form method="POST" action="{{ route('admin.operations.pictures.destroy',$picture) }}" onsubmit="return confirm('Are you sure you want to delete this picture?')">@csrf @method('DELETE')<button>Delete</button></form></div></div></article>@empty<p class="empty-state">No pictures uploaded yet.</p>@endforelse</div>{{ $pictures->links() }}@endsection
