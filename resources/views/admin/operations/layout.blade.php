<!doctype html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>@yield('title', 'Operations') | OneGoBike</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><div class="dashboard-shell d-flex"><aside class="sidebar"><div class="brand">OneGoBike <span>ADMIN</span></div><nav class="sidebar-nav"><a href="{{ route('admin.dashboard') }}">Live Map</a><a class="{{ request()->routeIs('admin.operations*') ? 'active' : '' }}" href="{{ route('admin.operations') }}">Operations</a><a href="#">Reports</a></nav></aside><main class="main-panel"><div class="topbar"><div><p class="eyebrow">FIELD OPERATIONS / ADMINISTRATION</p><h1>@yield('heading', 'Operations')</h1></div><form action="{{ route('logout') }}" method="POST">@csrf<button class="logout-button">Log out</button></form></div>@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert error">{{ session('error') }}</div>@endif @if($errors->any())<div class="alert error"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif @yield('content')</main></div></body></html>
