<form class="form-panel" action="{{ $action }}" method="POST" enctype="multipart/form-data">@csrf @if($method !== 'POST') @method($method) @endif <label>Title<input name="title" required value="{{ old('title',$news?->title) }}"></label><label>Content<textarea name="body" rows="10" required>{{ old('body',$news?->body) }}</textarea></label><label>Image<input type="file" name="image" accept="image/*">@if($news?->image_path)<small>Current: {{ $news->image_path }}</small>@endif</label><label>Status<select name="status"><option {{ old('status', $news?->is_published ? 'Published' : 'Draft') === 'Draft' ? 'selected' : '' }}>Draft</option><option {{ old('status', $news?->is_published ? 'Published' : 'Draft') === 'Published' ? 'selected' : '' }}>Published</option></select></label><div class="form-actions"><button class="primary-button">{{ $button }}</button><a class="map-button" href="{{ route('admin.operations.news.index') }}">Cancel</a></div></form>
