@extends('admin.operations.layout') @section('title','Users') @section('heading','User management') @section('content')<div class="page-actions"><form class="search-form"><input name="search" value="{{ request('search') }}" placeholder="Search name or email..."><select name="role"><option value="">All roles</option>@foreach($roles as $role)<option {{ request('role')===$role?'selected':'' }}>{{ $role }}</option>@endforeach</select><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option {{ request('status')===$status?'selected':'' }}>{{ $status }}</option>@endforeach</select><button class="map-button">Search</button></form><a class="primary-button" href="{{ route('admin.operations.users.create') }}">+ Add user</a></div><div class="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->role ?? ($user->is_admin ? 'Admin' : 'User') }}</td><td><span class="status-pill {{ strtolower($user->status ?? 'Active') }}">{{ $user->status ?? 'Active' }}</span></td><td class="actions"><a href="{{ route('admin.operations.users.show',$user) }}">View</a><a href="{{ route('admin.operations.users.edit',$user) }}">Edit</a>@if(!$user->is(auth()->user()))<form method="POST" action="{{ route('admin.operations.users.destroy',$user) }}" onsubmit="return confirm('Are you sure you want to delete this user?')">@csrf @method('DELETE')<button>Delete</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="empty-state">No users found.</td></tr>@endforelse</tbody></table></div>{{ $users->links() }}@endsection
